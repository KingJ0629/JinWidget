<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.widget">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.widget.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MinePersonalSettingsActivity"/>
        <activity android:name=".TailsTextLayoutTestActivity"/>
        <activity android:name=".TestLoadView"/>
        <activity android:name=".JRecyclerViewTest"/>
        <activity android:name=".SuperTextTestActivity"/>
    </application>

</manifest>